{
  "name": "vitrace",
  "version": "1.1.1",
  "description": "Vitrace — Refleksi Psikografis & Bazi",
  "private": true,
  "scripts": {
    "dev": "netlify dev",
    "check": "npm run check:backend && npm run check:frontend",
    "test": "node --test",
    "predeploy": "npm run check",
    "check:backend": "node --check netlify/functions/analyze.js",
    "check:frontend": "node --check assets/app.js"
  },
  "dependencies": {
    "openai": "4.67.1"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18"
  }
}
